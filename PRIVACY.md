# 隐私说明

最后更新：2026-08-24

## 一句话说明

门控程序在本机持续检查“小欧”和“结束”；门控关闭时只向虚拟麦克风输出静音，门控开启后才把真实麦克风音频送往用户已选择的 Codex 输入设备。

## 本项目处理什么数据

门控运行期间会处理：

- 真实麦克风采集的原始 PCM 音频。
- 本地关键词检测器返回的控制词事件。
- 音频设备名称、通道数和采样率等运行时设备信息。
- 运行状态和脱敏错误信息。

这些数据用于本机关键词识别、门控和音频路由。

## 音频在什么情况下离开本机

### 门控关闭

真实麦克风音频仍会在本机进入关键词检测器，以判断是否说出“小欧”。写入 VB-CABLE 的数据是静音。项目不会主动把关闭状态的真实音频发送到网络。

### 门控开启

真实麦克风音频经过短时延迟、门控和重采样后写入 `CABLE Input`。若 Codex 正在从 `CABLE Output` 读取，允许通过的音频将进入 Codex，后续处理由 Codex/OpenAI 的产品设置、账户设置和隐私政策决定。

### 门控停止

程序关闭音频输入和输出流，不再处理麦克风数据。操作者仍需退出 Codex 实时语音，并将 Codex 麦克风恢复为“系统默认”。

## 本项目不会做什么

项目运行时代码：

- 不保存录音文件。
- 不生成或保存全文转写。
- 不上传遥测或使用分析 SDK。
- 不创建云端账户。
- 不发送网络 API 请求。
- 不在 Windows 中注册后台服务或开机启动项。

安装依赖、下载模型、克隆仓库和安装 VB-CABLE 会分别访问 PyPI、GitHub、sherpa-onnx 文档或 VB-Audio 网站，这些下载不属于门控运行时的数据传输。

## 日志和错误信息

默认只在当前终端显示：

- 门控状态。
- 选中的音频设备名称和运行时编号。
- 预检结果。
- 异常类型和简短原因。

项目不会自动把终端输出写入日志文件。用户在提交 Issue 前应删除账户名、个人路径、设备序列号、录音和其他敏感信息。

## 第三方边界

- `sherpa-onnx` 在本机执行关键词检测。
- VB-CABLE 在 Windows 内部转发音频。
- Codex/OpenAI 接收门控开启后被放行的音频。
- 本项目不控制第三方组件或服务的隐私政策。

## 风险说明

关键词检测可能误唤醒或漏唤醒。门控一旦开启，麦克风范围内的全部声音都有可能被放行。不要把本项目用于医疗、生命安全、保密会议或其他要求确定性隔离的场景。

发现隐私问题时，请按照 [安全政策](SECURITY.md) 私密报告，不要在公开 Issue 中附带真实录音。
