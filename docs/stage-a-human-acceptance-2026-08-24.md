# 阶段 A 真人验收回执

日期：2026-08-24  
引擎：sherpa-onnx 开放词表关键词检测  
控制词：“小欧”开启，“结束”关闭  
结果：`10/10`，`complete=true`

## 通过场景

1. 关闭状态保持安静：无事件，保持关闭。
2. 关闭状态普通讲话：无事件，保持关闭。
3. 说“小陈”：无误唤醒，保持关闭。
4. 只说“小欧”：产生 `opened`，进入开启状态。
5. 开启后普通指令：无控制事件，保持开启。
6. 开启后连续两句话：无控制事件，保持开启。
7. 开启后再次说“小欧”：无重复开门事件，保持开启。
8. 只说“结束”：产生 `closed`，恢复关闭。
9. 关闭后普通讲话：无事件，保持关闭。
10. 完整周期“小欧—普通话—结束”：依次产生 `opened`、`closed`，最终关闭。

## 证据边界

- 真人验收由用户在本机交互运行并提供终端结果截图。
- 程序只输出场景编号、门控事件和最终状态；没有保存音频或全文转写。
- 此回执证明阶段 A 关键词检测和状态切换在人类口述测试中通过。
- 此回执不证明虚拟音频通道或 Codex 实时语音端到端链路已经完成；VB-CABLE
  尚未安装，系统默认麦克风和 Codex 设置均未修改。
